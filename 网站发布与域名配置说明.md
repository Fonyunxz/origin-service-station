# 网站发布与域名配置说明

当前网站已经按“主站、视频、实时接口分离”的方式整理：

- 主站：`index.html`、其他功能页面和 `服务站11-1-assets` 中的普通静态资源。
- 视频：由独立媒体域名提供，页面内直接播放。
- 实时接口：由独立 API 域名提供，接口慢或临时不可用时不会阻止普通页面打开。
- 域名配置：集中在 `服务站11-1-assets/site-config.js`。

## 域名确定后要修改的地方

打开 `服务站11-1-assets/site-config.js`，填写：

```js
publicOrigin: "https://www.你的域名.com",
mediaOrigin: "https://media.你的域名.com",
apiOrigin: "https://api.你的域名.com",
```

只填写 `publicOrigin` 不会自动搬运视频和接口。必须先把视频上传到媒体存储，把接口部署到 API 服务，再填写对应域名。

## 采用与 web3origin.com 相近的发布结构

1. 把整个网站目录上传到 GitHub 仓库。
2. 在仓库设置中启用 GitHub Pages。
3. 域名确定后，把 `CNAME.example` 复制为 `CNAME`，内容改成你的主域名。
4. 在 Cloudflare DNS 中配置主域名：
   - `185.199.108.153`
   - `185.199.109.153`
   - `185.199.110.153`
   - `185.199.111.153`
5. 为 `media` 子域名配置视频对象存储或 CDN。
6. 为 `api` 子域名配置实时数据服务。
7. 在 GitHub Pages 中开启 HTTPS。

## 国内稳定访问方案

如果主要服务中国大陆用户，建议把主站放在阿里云 OSS、腾讯云 COS 或大陆服务器，并在备案完成后接入国内 CDN。视频放到国内对象存储，API 放到国内云函数或服务器。页面代码和域名配置方式不需要改变。

## 当前兼容状态

在新的视频域名和 API 域名尚未配置前：

- 视频继续使用现有公开媒体源；
- 雷达、钱包与推荐关系查询继续使用现有公开接口；
- 网站可以继续通过本地文件打开；
- 域名确定后不需要逐页修改链接。
